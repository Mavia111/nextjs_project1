export const Para = (props:any) =>{

    return(
        <p className="para">{props.name}</p>
    );
}

export const SubPara = (props:any) =>{

    return(
        <p className="subpara">{props.name}</p>
    );
}